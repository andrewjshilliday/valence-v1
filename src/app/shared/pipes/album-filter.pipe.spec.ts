import { AlbumFilterPipe } from './album-filter.pipe';

describe('AlbumFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AlbumFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
