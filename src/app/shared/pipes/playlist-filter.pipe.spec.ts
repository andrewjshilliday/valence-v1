import { PlaylistFilterPipe } from './playlist-filter.pipe';

describe('PlaylistFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlaylistFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
